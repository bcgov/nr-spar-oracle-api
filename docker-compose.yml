version: '3.9'

services:
  backend:
    container_name: service-api
    image: service-api:latest
    hostname: service-api
    ports:
      - "8090:8090"
    build:
      context: .
      dockerfile: Dockerfile
